function getTime(){
    // get Hour and rest seconds
    const hour = parseInt(time/3600);
    let remainingSecod = time % 3600;
    const minutes = parseInt(remainingTime / 3600)
    remainingSecod = 

}